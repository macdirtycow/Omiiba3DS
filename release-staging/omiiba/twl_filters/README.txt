Optional TWL filter storage
===========================

Put experimental or backup TWL filter files here if you want to keep them with
your Omiiba setup.

The active filter path used by Omiiba/Luma is:

  SD:/omiiba/twl_upscaling_filter.bin

DS Widescreen Labs checks that active path and can enable the matching
"Enable DSi external filter" setting.
